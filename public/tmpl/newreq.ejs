<!DOCTYPE html>
<html data-ng-app="cubesatApp">
<% include header %>
<% include navbar %>

<body class="container" data-ng-controller="newreqController">
    <h1>Post a New Requirement</h1>
    <p class="lead">Fill out the form to add a new requirement to a corresponding component.</p>
    <hr>
    <div class="row">
        <div class="col-lg-12">
            <h3>Name: </h3>
                <input type="text" required class="form-control" placeholder="e.g., Attitude Control" ng-model="inputName"/>
            <div class="divider"></div>

            <h3>Description: </h3>
                <input type="text" class="form-control" placeholder="e.g., The ADCS will control the attitude of s/c." ng-model="inputDescription"/>
            <div class="divider"></div>

            <h3>Rationalization: </h3>
                <textarea class="form-control" rows="6" placeholder="e.g., The primary mission objectives require the use of an attitude determination/control subsystem." ng-model="inputRationalization"></textarea>
            <div class="divider"></div>

            <h3>Verification: </h3>
                <input type="text" class="form-control" placeholder="e.g., The ADCS will be verified through..." ng-model="inputVerification"/>
            <div class="divider"></div>

            <h3>Type of Requirement:</h3>
                <select multiple class="form-control" size="6">
                    <option ng-click="saveReqType('State')">State/Mode</option>
                    <option ng-click="saveReqType('Functional')">Functional</option>
                    <option ng-click="saveReqType('Performance')">Performance</option>
                    <option ng-click="saveReqType('External interface')">External Interface</option>
                    <option ng-click="saveReqType('Environmental')">Environmental</option>
                    <option ng-click="saveReqType('Physical')">Physical</option>
                </select>
            <div class="divider"></div>

            <h3>Component Tracing:</h3>
            <div>
                <ul class="nav nav-tabs">
                    <li class="active"><a href="#tab1">Top-Level</a></li>
                    <li>               <a href="#tab2">ADCS</a></li>
                    <li>               <a href="#tab3">CDH</a></li>
                    <li>               <a href="#tab4">EPS</a></li>
                    <li>               <a href="#tab5">PS</a></li>
                    <li>               <a href="#tab6">TS</a></li>
                    <li>               <a href="#tab7">COM</a></li>
                </ul>
            </div>
            <br>
            <section id="tab7" class="tab-content hide">
                <select multiple class="form-control" size="6">
                    <option ng-click="saveProduct(product.subsystem, product._id)" ng-repeat ="product in products | orderBy:predicate:reverse" ng-if="product.subsystem == 'com'">{{product.name}}</option>
                </select>
            </section>

            <section id="tab6" class="tab-content hide">
                <select multiple class="form-control" size="6">
                    <option ng-click="saveProduct(product.subsystem, product._id)" ng-repeat ="product in products | orderBy:predicate:reverse" ng-if="product.subsystem == 'ts'">{{product.name}}</option>
                </select>
            </section>

            <section id="tab5" class="tab-content hide">
                <select multiple class="form-control" size="6">
                    <option ng-click="saveProduct(product.subsystem, product._id)" ng-repeat ="product in products | orderBy:predicate:reverse" ng-if="product.subsystem == 'ps'">{{product.name}}</option>
                </select>
            </section>

            <section id="tab4" class="tab-content hide">
                <select multiple class="form-control" size="6">
                    <option ng-click="saveProduct(product.subsystem, product._id)" ng-repeat ="product in products | orderBy:predicate:reverse" ng-if="product.subsystem == 'eps'">{{product.name}}</option>
                </select>
            </section>

            <section id="tab3" class="tab-content hide">
                <select multiple class="form-control" size="6">
                    <option ng-click="saveProduct(product.subsystem, product._id)" ng-repeat ="product in products | orderBy:predicate:reverse" ng-if="product.subsystem == 'cdh'">{{product.name}}</option>
                </select>
            </section>

            <section id="tab2" class="tab-content hide">
                <select multiple class="form-control" size="6">
                    <option ng-click="saveProduct(product.subsystem, product._id)" ng-repeat ="product in products | orderBy:predicate:reverse" ng-if="product.subsystem == 'adcs'">{{product.name}}</option>
                </select>
            </section>

            <section id="tab1" class="tab-content active">
                <select multiple class="form-control" size="6">
                    <option ng-click="saveProduct(product.subsystem, product._id)" ng-repeat ="product in products | orderBy:predicate:reverse" ng-if="product.subsystem == 'dubs'">{{product.name}}</option>
                </select>
            </section> 
            <div class="divider"></div>

        </div> 
    </div> 
    <div class="row">
        <div class="col-lg-12">
            <a type="button" class="btn btn-primary btn-lg" ng-click="addReq()" id="pushdown2" title= "Add Info"><h4>Add Requirement</h4></a>
        </div>
    </div>
    <hr>



</body>
</html>